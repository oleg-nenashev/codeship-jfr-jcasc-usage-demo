jenkinsfileRunner:
  image: jenkins/jenkinsfile-runner:1.0-beta-17
  volumes:
    - .:/workspace
    - jenkins.yaml:/jenkinsfile-runner/config.yaml
  environment:
    - CASC_JENKINS_CONFIG=/jenkinsfile-runner/config.yaml
